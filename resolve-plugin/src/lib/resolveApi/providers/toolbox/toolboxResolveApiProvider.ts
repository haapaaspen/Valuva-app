import { ResolveApiProvider } from "../resolveApiProvider";

export class ToolboxResolveApiProvider implements ResolveApiProvider {
    constructor() {
        console.log("ToolboxResolveApiProvider initialized");
    }
}